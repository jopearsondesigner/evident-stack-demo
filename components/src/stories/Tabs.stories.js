import Tabs from '../lib/tabs/TabsExample.svelte';

export default {
	title: 'website/Tabs',
	component: Tabs,
	parameters: {
		layout: 'fullscreen'
	},
	argTypes: {}
};

// eslint-disable-next-line no-unused-vars
const Template = (/** @type {any} */ args) => ({});

export const Default = Template.bind({});
// @ts-ignore
Default.args = {};
