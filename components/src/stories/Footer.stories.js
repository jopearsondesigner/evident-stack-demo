import Footer from '../lib/footer/FooterExample.svelte';

export default {
	title: 'website/Footer',
	component: Footer,
	parameters: {
		layout: 'fullscreen'
	},
	argTypes: {}
};

// eslint-disable-next-line no-unused-vars
const Template = (/** @type {any} */ args) => ({});

export const Default = Template.bind({});
// @ts-ignore
Default.args = {};
