import Placement from '../../lib/design/grid/Placement.svelte';

export default {
	title: 'Evident Design/Cursor',
	component: Placement,
	parameters: {
		layout: 'fullscreen'
	},
	argTypes: {}
};

// eslint-disable-next-line no-unused-vars
const Template = (/** @type {any} */ args) => ({});

export const Default = Template.bind({});
// @ts-ignore
Default.args = {};
