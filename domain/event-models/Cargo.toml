[package]
name = "event-models"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_cbor = "0.11"
url = "2.3"

[dependencies.uuid]
version = "1.2.2"
features = [
    "v4",
    "v5",
    "fast-rng",
    "macro-diagnostics",
]
