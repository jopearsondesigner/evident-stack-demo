import Modal from '../lib/ModalImportExample.svelte';

export default {
	title: 'product/Modal',
	component: Modal,
	parameters: {
		layout: 'fullscreen'
	},
	argTypes: {}
};

// eslint-disable-next-line no-unused-vars
const Template = (/** @type {any} */ args) => ({});

export const Default = Template.bind({});
// @ts-ignore
Default.args = {};
