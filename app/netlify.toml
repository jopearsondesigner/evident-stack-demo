[build]
  base = "app"
  command = "curl https://sh.rustup.rs -sSf | sh -s -- -y && . $HOME/.cargo/env && rustup default stable && curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh && npm run build"
  publish = "build"