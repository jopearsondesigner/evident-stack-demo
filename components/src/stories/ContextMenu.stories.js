import ContextMenu from '../lib/utils/ContextMenu.svelte';

export default {
	title: 'product/Context Menu',
	component: ContextMenu,
	parameters: {
		layout: 'fullscreen'
	},
	argTypes: {}
};

// eslint-disable-next-line no-unused-vars
const Template = (/** @type {any} */ args) => ({});

export const Default = Template.bind({});
// @ts-ignore
Default.args = {};
